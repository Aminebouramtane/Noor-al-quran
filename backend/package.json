{
  "name": "noor-al-quran-backend",
  "version": "1.0.0",
  "description": "Noor Al-Quran Backend API - Tajweed Learning with Audio Datasets",
  "type": "module",
  "scripts": {
    "dev": "python -m uvicorn main:app --reload --host 0.0.0.0 --port 8000",
    "start": "python -m uvicorn main:app --host 0.0.0.0 --port 8000",
    "lint": "pylint *.py",
    "test": "pytest"
  },
  "dependencies": {
    "fastapi": "^0.104.0",
    "uvicorn": "^0.24.0",
    "firebase-admin": "^6.2.0",
    "pandas": "^2.1.0",
    "python-dotenv": "^1.0.0",
    "pydantic": "^2.4.0",
    "python-multipart": "^0.0.6",
    "librosa": "^0.10.0",
    "soundfile": "^0.12.1"
  }
}
